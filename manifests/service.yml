apiVersion: v1
kind: Service
metadata:
    name: "positivemustangaks-aa1a"
    labels:
        app: "positivemustangaks-aa1a"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "positivemustangaks-aa1a"